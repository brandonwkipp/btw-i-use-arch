---
- hosts: localhost
  become: yes
  roles: 
    - { role: base, tags: ['base']}
    - { role: user, tags: ['user']}
    - { role: audio, tags: ['audio']}
    - { role: services, tags: ['services']}
